"""
Given a graph G = (V,E) as input, create and identical graph G' = (V', E'),
where the weight of each vertex in V' is identical to the weight of each
vertex in V and every edge in E' connects the same nodes as it's mirror edge in
E.

tl;dr; Create a graphy G' that is identical to the graph G given as inputQueen
"""
